<?
 /**
 * @desc The index action
 *
 * @author Majisti
 */

$content = pq('<div />')
    ->append(pq('<a />')
        ->attr('href', $this->url(array('controller' => 'auth')))
        ->html("Auth")
    )
    ->append(pq('<br />'))

    /* editors */
    ->append($this->editing('foo'))
    ->append($this->editing('foo2', 'basic'))
;

$this->setRenderReturn($content);
