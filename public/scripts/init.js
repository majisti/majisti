var majisti;

function majisti_init(conf)
{
    majisti = {
        app: {
            appUrl: conf.appUrl,
            baseUrl: conf.baseUrl,
            currentUrl: conf.currentUrl
        },
        ext : {}
    };
}